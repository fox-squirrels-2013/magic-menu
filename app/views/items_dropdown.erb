<select id="item_selector">
  <option selected="selected">Menu Items</option>
  <% @all_items.each do |item| %>
    <option value="<%= item.id %>"><%= "#{item.name} - #{item.price}" %></option>
  <% end %>
</select>