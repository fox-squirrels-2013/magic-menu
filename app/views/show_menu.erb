<a href="/menus">Back to Menus!</a>
</br>
</br>
<h3><%= @menu.name %> Menu </h3>
<!-- We use the data attribute to hold on to information we want to access later ... data-menu -->
<div id="menu-id" data-menu="<%= @menu.id %>"></div>
<div class="menu-items"> Items click to the RIGHT of the item name, in the whitespace, to delete. Feature- not a bug.  Avoid temptation to hit the red x.  Thank you. </div>
 <% @menu.items.each do |item| %>
 <div class="menu-item" id="<%=@menu.id%>_<%=item.id%>">
 	<span class="btn-delete"><a href="#" onclick=<% @delete %>>[x]</a></span>
 	<span class="item"><%= item.name %></span>
 </div>
 <% end %>
<!-- This is where we add the new item to the list  we had to do it this way because adding it to the each loop above would add the 'new-item' to every item.-->
 <div class='new-item'>

 </div>

<!-- ###Menu items -->
</br>
</br>
</br>
</br>
</br>

<div class="all-items"></div>
<select id="Menu-Items" >
  <option value="display">Menu-Items</option>
  <% @items.each do |item| %>
    <option value="<%=item.id%>">
      <%= item.name%><%= item.price.to_f/100 %>
    </option>
  <% end %>
</select>

